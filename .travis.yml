language: python
python:
  - "3.7"

# command to install dependencies
install:
  - pip install -r requirements.txt && pip install coverage
# command to run tests
script:
  - coverage run tests/test.py &&  coverage report -m --omit "env/*","*test.py","/virtualenv/"